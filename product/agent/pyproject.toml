[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ripple-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pydantic>=2,<3"]

[tool.setuptools]
packages = ["ripple_agent"]
